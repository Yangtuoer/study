#include <iostream>

using namespace std;

int main()
{
    int n;
    while(cin >> n)
    {
        int i = 0;
        while(n != 0){
            n = n & (n << 1);
            i++;
        }
        cout << i << endl;
    }
}
